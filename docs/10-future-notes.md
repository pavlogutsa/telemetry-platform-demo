# Future Notes

- Partitioning / archiving strategy for device_status_history
- Debezium CDC from Oracle into Kafka
- Multi-tenant separation (orgId per device)
- JWT/OIDC validation at ingress instead of static API keys
